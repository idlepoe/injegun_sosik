군장병우대업소화면

drawer 마지막 메뉴에 표시 

군장병우대업소(상품권환급)

collection soldiers를 표시하는 화면


appbar actions에 information 아이콘 버튼에 누를 경우

환급대상 : 군사병에 한함 *부사관 등 간부급 제외     << 검정 타이틀 


* 휴가증, 외출·외박증 등 지참                    << 이하 파란색으로 표시
* 나라사랑카드 결제시에만 환급 가능
* 군사병 1명 최대 5만원까지 환급 가능
* 결제금액별 환급 시행
* 2026.4.1.부터 인제채워드림카드 인센티브로 환급 시행
(군 장병 인제채워드림카드 소지 및 “그리고”어플 등록 必)

표시 후 하단에 [원본 페이지 보기] 버튼 

https://www.inje.go.kr/portal/inje-news/soldier/givePreference



상단엔 아이콘과 label의 버튼이 표시.

default는 음식점 restaurant 

선택되어 있는 아이콘은 outline 파란색 강조표시 

[음식점] [숙박업]
[미용실] [PC방]
----------------  sliverAppbar로 스크롤시 상단이 감춰짐.
[photoUrl 없다면 아이콘] 상호명               맵 아이콘 선택시 naver map 이동
                       주소명


----------------
최하단에 [가까운 순으로 표시] 버튼 표시 선택시 

사용자 위치 정보 취득 후 거리순으로 정렬하고 맵 아이콘 하단에 km로 현재로부터의 거리 표시

  Future<_LocationAccessResult> _getUserLocation() async {
    try {
      // 위치 권한 확인
      var permission = await Geolocator.checkPermission();
      if (permission == LocationPermission.denied) {
        permission = await Geolocator.requestPermission();
      }

      if (permission == LocationPermission.denied) {
        return _LocationAccessResult.denied;
      }

      if (permission == LocationPermission.deniedForever) {
        return _LocationAccessResult.deniedForever;
      }

      // 위치 서비스 활성화 확인
      final serviceEnabled = await Geolocator.isLocationServiceEnabled();
      if (!serviceEnabled) {
        return _LocationAccessResult.serviceDisabled;
      }

      // 현재 위치 가져오기
      final position = await Geolocator.getCurrentPosition(
        desiredAccuracy: LocationAccuracy.high,
      );

      userPosition.value = position;
      return _LocationAccessResult.success;
    } catch (e) {
      print('Error getting user location: $e');
      return _LocationAccessResult.error;
    }
  }

위치 정보 취득 실패시

_LocationAccessResult.deniedForever 일때 

location_permission_denied_message 메시지 다이얼로그 

await Geolocator.openAppSettings(); 로 이동하는 버튼 표시


그 외엔 location_permission_error 실패했다는 메시지 다이얼로그

await Geolocator.openAppSettings(); 로 이동하는 버튼 표시


List의 onTap 은 bottomSheet 로 photoUrl을 표시, name, district, address, embedMap, updatedAt 을 표시

embedMap은 하단 코드를 참고

Padding(
            padding: const EdgeInsets.symmetric(horizontal: 8.0),
            child: SizedBox(
              height: 220,
              child: PlatformMap(
                initialCameraPosition: CameraPosition(
                  target: LatLng(latitude!, longitude!),
                  zoom: 16.0,
                ),
                markers: Set<Marker>.of([
                  Marker(
                    markerId: MarkerId('event_location'),
                    position: LatLng(latitude!, longitude!),
                    consumeTapEvents: true,
                    infoWindow: InfoWindow(
                      title: title ?? location,
                      snippet: address ?? location,
                    ),
                  ),
                ]),
                mapType: MapType.normal,
                compassEnabled: true,
              ),
            ),
          ),