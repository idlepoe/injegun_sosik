매일 6시부터 ~ 18시까지 3시간마다
fetchWeather
fetchWeekschedule
fetchLivelihood
fetchJob
fetchFree
fetchNotice

해당 처리 이후

새로 저장(create)된 취득한 내용의 가장 최신 정보 + 건수를 topic push메시지로 발송

매일 6시와 18시에
fetchNewsletters
fetchDashboardSlider

해당 스케쥴을 functions에 추가
